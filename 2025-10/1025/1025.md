# 📝 알고리즘 문제 풀이 - 2025-10-25

## 문제 📖

- **문제 링크:** [prog - 이중우선순위큐](https://school.programmers.co.kr/learn/courses/30/lessons/42628)

- **문제 유형:** 힙

- **난이도:** Lv.3

- **풀이 시간:** ⏳ 20분

## ✍ 접근 방식

- (어떤 방법으로 접근했는지 설명)
- (다른 풀이를 참고했다면 어떤걸 배웠는지)

- split 을 사용해야하는데 slice를 사용함;
- 입력값이 string인 경우 Number로 사용해야하는 경우를 주의할것. 타입에 주의!
- 예외처리 ex.D 연산자 실행시 빈 배열일경우 넘겨야함.

- forEach vs map

  - forEach는 각 요소에 대해 부수효과를 갖음. 반환값이 없음. undefined
  - map 은 반환값들로 새로운 배열을 만들어 반환.

- find : 조건에 만족하는 첫 요소를 반환
- findIndex : 조건을 만족하는 첫요소의 index 반환
  ```js
  const idx = queue.findIndex((x) => x === mx);
  queue.splice(idx, 1);
  ```
- some : 조건을 만족하면 즉시 true 반환

  ```js
  queue.some((x, idx) => {
    if (x === mi) {
      queue.splice(idx, 1);
      return true;
    }
    return false;
  });
  ```
