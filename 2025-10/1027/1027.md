# 📝 알고리즘 문제 풀이 - 2025-10-25

## 문제 📖

- **문제 링크:** [prog - 네트워크](https://school.programmers.co.kr/learn/courses/30/lessons/43162)

- **문제 유형:** DFS

- **난이도:** Lv.3

- **풀이 시간:** ⏳ 40분

## ✍ 접근 방식

- (어떤 방법으로 접근했는지 설명)
- (다른 풀이를 참고했다면 어떤걸 배웠는지)

```js
function solution(n, computers) {
  const nets = []; //Set들의 배열

  const findSetIndex = (node) => {
    for (let k = 0; k < nets.length; k++) {
      if (nets[k].has(node)) return k;
    }
    return -1;
  };

  for (let i = 0; i < n; i++) {
    for (let j = i + 1; j < n; j++) {
      if (computers[i][j] === 1) {
        const idxI = findSetIndex(i);
        const idxJ = findSetIndex(j);

        if (idxI === -1 && idxJ === -1) {
          // 둘 다 속한 set이 없으면 새로 생성
          nets.push(new Set([i, j]));
        } else if (idxI !== -1 && idxJ === -1) {
          // i가 있는 set에 j 추가
          nets[idxI].add(j);
        } else if (idxJ !== -1 && idxI === -1) {
          // j가 있는 set에 i 추가
          nets[idxJ].add(i);
        } else if (idxI !== idxJ) {
          // 서로 다른 set이면 병합 (작은 인덱스에 합치고 큰 인덱스 삭제)
          const a = Math.min(idxI, idxJ);
          const b = Math.max(idxI, idxJ);
          for (const v of nets[b]) nets[a].add(v);
          nets.splice(b, 1);
        }
        // idxI === idxJ이면 이미 같은 네트워크
      }
      // computers[i][j] === 0일 때는 아무 행동 안 함 (0마다 새 Set 만들면 안 됨)
    }
  }
  for (let i = 0; i < n; i++) {
    if (findSetIndex(i) === -1) nets.push(new Set([i]));
  }

  return nets.length;
}
```
